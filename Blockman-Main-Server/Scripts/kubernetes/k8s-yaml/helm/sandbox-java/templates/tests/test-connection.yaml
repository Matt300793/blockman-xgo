{{- if .Values.sidecar.enabled -}}
apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sandbox-java.fullname" . }}-test-connection"
  labels:
    {{- include "sandbox-java.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sandbox-java.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
{{- end }}