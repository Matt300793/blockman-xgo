<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:bind="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
            name="DeleteMapViewModel"
            type="com.sandboxol.mapeditor.view.fragment.deletemap.DeleteMapViewModel"/>

    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <com.sandboxol.common.widget.rv.pagerv.PageRecyclerView
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            bind:model="@{DeleteMapViewModel.deleteMapListModel}"/>

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="#FBF1E2">

            <CheckBox
                style="@style/CircleCheckBoxStyle"
                android:layout_height="match_parent"
                android:checked="@{DeleteMapViewModel.isAllSelected}"
                android:clickable="true"
                android:drawablePadding="@dimen/space10dp"
                android:paddingLeft="@dimen/space12dp"
                android:text="@string/map_all_select"
                android:textColor="#555555"
                bind:onClickCommand="@{DeleteMapViewModel.onAllClickCommand}"/>

            <Button
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginBottom="@dimen/space8dp"
                android:layout_marginRight="@dimen/space12dp"
                android:layout_marginTop="@dimen/space8dp"
                android:background="@drawable/btn_main_confirm"
                android:enabled="@{DeleteMapViewModel.isEnableDelete}"
                android:text="@string/delete_map_title"
                android:textSize="@dimen/textSize15Sp"
                bind:onClickCommand="@{DeleteMapViewModel.onDeleteCommand}"/>

        </LinearLayout>

    </LinearLayout>


</layout>